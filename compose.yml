services:
  db:
    image: postgres:16-alpine
    container_name: coopDB
    environment:
      POSTGRES_DB: cooperativa_db
      POSTGRES_USER: yimysito
      POSTGRES_PASSWORD: tarqui231B
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./bd.sql:/docker-entrypoint-initdb.d/bd.sql:ro
    ports:
      - "5432:5432"

volumes:
  pg_data: